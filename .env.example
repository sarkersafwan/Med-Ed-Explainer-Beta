LLM_PROVIDER=openai          # Shared text/vision provider abstraction (currently openai)
OPENAI_API_KEY=              # Text + review + image-prompt generation
OPENAI_MODEL=gpt-4o          # Main text model
OPENAI_VISION_MODEL=gpt-4o   # Vision model for style-reference analysis
PIPELINE_MODE=auto           # auto => production for PDFs, creative for topic-only runs
AIRTABLE_PAT=                # Airtable Personal Access Token
AIRTABLE_BASE_ID=appjmdOqi7hTArDN6
ELEVENLABS_API_KEY=          # ElevenLabs TTS
ELEVENLABS_VOICE_ID=         # ElevenLabs voice ID (or use --voice-id flag)
GEMINI_API_KEY=              # Nano Banana images (500 free/day) + image analysis
GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview  # Nano Banana Pro (reference-image support). Use imagen-4.0-ultra-generate-001 to roll back.
WAVESPEED_API_KEY=           # InfiniteTalk avatar ONLY
KIE_API_KEY=                 # Kling 3.0 video generation ONLY
